## Add option to publish files without login
<!--
type: feature
scope: all
affected: all
-->

The new CLI argument `--public` generates HTML instead of PHP files.
Additionally the content of `overview` is saved as `index` to get direct access.

This fixes #3
